# Legacy SpecKit Agents

This directory belongs to the previous SpecKit workflow.

SpecKit is no longer active for new Shift work. gentle SDD is the active workflow, and repo-root [AGENTS.md](../../AGENTS.md) is the tracked authority for agent workflow.

These agent files remain only as historical/reference workflow context. Do not use them to start new work unless you are explicitly performing historical analysis or migration.
