# UI Package

Shared Tailwind configuration and shadcn/ui primitives for Shift applications.

## Design Tokens

- `src/tailwind-preset.ts` mirrors the Tamagui marketing tokens (colors, spacing, radii, font stack, typography scale).
- Update Tamagui values first, then propagate here so Shift apps stay visually consistent.
